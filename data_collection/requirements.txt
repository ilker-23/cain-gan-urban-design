# Veri toplama için ek bağımlılıklar
# (ana requirements.txt'deki Pillow, numpy zaten gerekli)

requests>=2.31.0

# Coğrafi veri işleme (opsiyonel ama önerilen — DEM/sismik için)
rasterio>=1.3.0
geopandas>=0.13.0
shapely>=2.0.0

# Hızlandırma (opsiyonel)
tqdm>=4.65.0
